#include <secure_gateway.h>

void transformer_encode(struct message_t* msg __attribute__((unused)))
{
    /* do nothing */
}

void transformer_decode(struct message_t* msg __attribute__((unused)))
{
    /* do nothing */
}
